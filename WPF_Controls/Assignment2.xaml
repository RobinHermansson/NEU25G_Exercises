<Window x:Class="WPF_Controls.Assignment2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Controls"
        mc:Ignorable="d"
        Title="Assignment2" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Bottom" >
        <Label HorizontalAlignment="Center" VerticalAlignment="Center" Content="Counter: "/>
            <Label HorizontalAlignment="Center" VerticalAlignment="Center" x:Name="CounterLabel" Content="0"/>
        </StackPanel>
        
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top">
            <Button Content="+" Click="ButtonPlus_Click" Margin="0 0 50 0" Padding="20"/>
            <Button Content="-" Click="ButtonMinus_Click" Margin="0 0 0 0" Padding="20"/>
        </StackPanel>
        
    </Grid>
</Window>
